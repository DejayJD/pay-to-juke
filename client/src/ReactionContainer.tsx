import { Box } from "@audius/harmony"


const FloatingReaction = ({type}:{type: ReactionType})

export const ReactionHandler = () => {
    const [reactions, setReactions] = useState<{>([]) 
    return <Box />
}